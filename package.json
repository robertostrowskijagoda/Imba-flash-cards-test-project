{
  "name": "imba-flash-cards-test-project",
  "scripts": {
    "dev": "imba -w ./src/server/server.imba",
    "build": "imba build ./src/server/server.imba",
    "preview": "node dist/server.js",
    "prod": "npx pm2 start dist/server.js",

    "start": "npx pm2 stop 0 & npm run build & npx pm2 start dist/server.js",
    "stop": "npx pm2 stop 0"
  },
  "dependencies": {
    "express": "^5.1.0",
    "better-sqlite3": "^9.0.0"
  },
  "devDependencies": {
    "imba": "^2.0.0-alpha.247"
  }
}
