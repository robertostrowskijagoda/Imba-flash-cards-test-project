{
  "name": "imba-flash-cards-test-project",
  "scripts": {
    "dev": "imba -w ./src/server/server.imba",
    "build": "imba build ./src/server/server.imba",
    "preview": "node dist/server.js",
    "stop": "npx --yes kill-port 8080",
    "start": "npm run stop && npm run build && npm run preview"
  },
  "dependencies": {
    "hono": "^4.6.10",
    "@hono/node-server": "^1.19.0",
    "magic-sdk": "^29.4.2",
    "@magic-sdk/admin": "^2.4.1",
    "jsonwebtoken": "^9.0.2",
    "better-sqlite3": "^9.0.0"
  },
  "devDependencies": {
    "imba": "^2.0.0-alpha.237"
  }
}
